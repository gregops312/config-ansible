---
- name: Include Virtual box
  ansible.builtin.include_tasks: "{{ ansible_facts['os_family'] | lower }}_virtual_box.yml"
  when:
    - ansible_facts['os_family'] == "Debian"
    - ansible_facts['architecture'] in ["x86_64", "amd64"]
